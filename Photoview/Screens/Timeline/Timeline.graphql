query timeline($limit: Int!, $offset: Int!) {
	myTimeline(paginate: {limit: $limit, offset: $offset}) {
		id
		date
		thumbnail {
			url
			width
			height
		}
		favorite
		album {
			id
			title
		}
	}
}
